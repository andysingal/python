row1 = ["⬜️","⬜️","⬜️"]
row2 = ["⬜️","⬜️","⬜️"]
row3 = ["⬜️","⬜️","⬜️"]
map = [row1, row2, row3]
print(f"{row1}\n{row2}\n{row3}")
#position = input("Where do you want to put the treasure? ")
# 🚨 Don't change the code above 👆
#horizontal = int(position[0]) 
#vertical = int(position[1])
#map[1-vertical][1-horizontal] = "X"
import random
x = random.randint(0, len(map) - 1)  # This picks a random row
y = random.randint(0, len(row1) - 1)
map[x][y] = 'X'
 
#map_choice = random.randint(0, len(map) - 1)
#map[1-vertical][1-horizontal][map_choice] == 'X'
#print(f"The position is : {map_choice}")
 
 
 
 
#Write your code above this row 👆
 
# 🚨 Don't change the code below 👇
print(f"{row1}\n{row2}\n{row3}")
